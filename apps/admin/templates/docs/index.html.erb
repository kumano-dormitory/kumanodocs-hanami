<div class="p-strip--light is-shallow u-no-padding--bottom">
  <div class="row">
    <ul class="p-breadcrumbs">
      <li class="p-breadcrumbs__item"><a href='<%= routes.root_path %>'>管理者向けページ</a></li>
      <li class="p-breadcrumbs__item"><strong>寮生向けの資料一覧</strong></li>
    </ul>
    <hr/>
  </div>
</div>
<div class="p-strip is-shallow">
  <div class="row">
    <h2>寮生向けの部会・委員会の資料の一覧</h2>
    <a href="<%= routes.new_doc_path %>" class="p-button--positive">資料の追加</a>
    <a href="<%= routes.order_docs_path %>" class="p-button--positive">資料の並び替え</a>
    <ul class="p-list--divided">
      <% documents.each do |document| %>
      <li class="p-list__item">
        <a href="<%= routes.doc_path(document.id) %>" class="p-button--base u-no-margin--bottom">
          <h4 class="u-no-margin--bottom full-width"><%= document.title %><small>(文責:<%= document.user.name %>)</small></h4>
        </a>
      </li>
      <% end %>
    </ul>
  </div>
</div>
