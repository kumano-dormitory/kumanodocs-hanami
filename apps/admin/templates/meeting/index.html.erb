<p><a href='<%= routes.root_path %>'>Admin top page(this page)</a></p>
<p><a href='<%= routes.new_meeting_path %>'>ブロック会議日程を新規作成</a></p>
<div>
  <ul>
    <% meetings.each do |meeting| %>
      <li><a href='<%= routes.meeting_path(id: meeting.id) %>'><%= meeting.date %></a></li>
    <% end %>
  </ul>
</div>
