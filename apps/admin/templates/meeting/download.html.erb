<% if view_type == :meetings %>

<div class="p-strip--light is-shallow u-no-padding--bottom">
  <div class="row">
    <ul class="p-breadcrumbs">
      <li class="p-breadcrumbs__item"><a href='<%= routes.root_path %>'>管理者向けページ</a></li>
      <li class="p-breadcrumbs__item"><strong>印刷用PDFのダウンロード（会議日程の選択）</strong></li>
    </ul>
    <hr/>
  </div>
</div>
<div class="p-strip is-shallow">
  <div class="row">
    <h2>印刷用PDFのダウンロード</h2>
    <p>会議日程を選んでください</p>
    <ul class="p-list--divided">
      <% meetings.each do |meeting| %>
      <li class="p-list__item">
        <a href='<%= routes.download_meeting_path(id: meeting.id) %>' class="p-button--base u-no-margin--bottom">
          <h4 class="u-no-margin--bottom"><%= meeting.formatted_date %>の<%= meeting.type == 0 ? "ブロック会議" : "寮生大会" %></h4>
        </a>
      </li>
      <% end %>
    </ul>
  </div>
</div>

<% elsif view_type == :meeting %>

<div class="p-strip--light is-shallow u-no-padding--bottom">
  <div class="row">
    <ul class="p-breadcrumbs">
      <li class="p-breadcrumbs__item"><a href='<%= routes.root_path %>'>管理者向けページ</a></li>
      <li class="p-breadcrumbs__item"><a href='<%= routes.download_meeting_path(id: 0) %>'>印刷用PDFのダウンロード（会議日程の選択）</a></li>
      <li class="p-breadcrumbs__item"><strong><%= meeting.formatted_date %>の<%= meeting.type == 0 ? "ブロック会議" : "寮生大会" %></strong></li>
    </ul>
    <hr/>
  </div>
</div>
<div class="p-strip is-shallow">
  <div class="row">
    <h2>印刷用PDFのダウンロード</h2>
    <h3><%= meeting.formatted_date %>の<%= meeting.type == 0 ? "ブロック会議" : "寮生大会" %></h3>
    <p>印刷する資料の種類を選んでください</p>
    <ul class="p-list">
      <li class="p-list__item">
        <a href='<%= routes.edit_article_number_path(id: meeting.id) %>?download=true' class="p-button--positive">
          <h4 class="u-no-margin--bottom"><%= meeting.type == 0 ? "ブロック会議" : "寮生大会" %>資料</h4>
        </a>
      </li>
      <li class="p-list__item">
        <a href='<%= routes.download_meeting_path(id: meeting.id) %>?comments=true' class="p-button--brand">
          <h4 class="u-no-margin--bottom">議事録一覧(旧 報告会資料)</h4>
        </a>
      </li>
    </ul>
  </div>
</div>
<% else %>

<% end %>
