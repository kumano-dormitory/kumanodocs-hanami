<!DOCTYPE html>
<html>
  <head>
    <title>Admin</title>
    <%= favicon %>
    <link rel="stylesheet" href="https://assets.ubuntu.com/v1/vanilla-framework-version-1.8.1.min.css" />
  </head>
  <body>
    <header id="navigation" class="p-navigation" style="background-color: #ff9e80">
      <div class="row">
        <div class="p-navigation__banner">
          <div class="p-navigation__logo">
            <a class="p-navigation__link" href="/admin">
              <img src="/assets/logo.png" alt="" class="p-navigation__image">
            </a>
          </div>
          <a href="#navigation" class="p-navigation__toggle--open" title="menu">Menu</a>
          <a href="#navigation-closed" class="p-navigation__toggle--close" title="close menu">Close menu</a>
        </div>
        <nav class="p-navigation__nav" role="menubar">
          <span class="u-off-screen">
            <a href="#main-content">Jump to main content</a>
          </span>
          <ul class="p-navigation__links" role="menu">
            <li class="p-navigation__link <%= navigation[:root] ? 'is-selected' : '' %>" role="menuitem"><a href="/admin">管理者向けトップページ</a></li>
            <li class="p-navigation__link <%= navigation[:meeting] ? 'is-selected' : '' %>" role="menuitem"><a href="/admin/meeting">ブロック会議一覧</a></li>
            <li class="p-navigation__link <%= navigation[:pdf] ? 'is-selected' : '' %>" role="menuitem"><a href="/">印刷用PDFダウンロード</a></li>
            <li class="p-navigation__link" role="menuitem"><a href="/admin/logout"><i class="p-icon--user"></i>ログアウト（一般向けページへ）</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <div class="u-no-margin--top">
      <% unless notifications.empty? %>
        <% p notifications %>
        <div class="p-strip--accent is-shallow">
          <div class="row">
            <div class="col-12">
              <% if notifications[:error] %>
                <div class="p-notification--negative">
                  <p class="p-notification__response">
                    <span class="p-notification__status"><%= notifications[:error][:status] %></span>
                    <%= notifications[:error][:message] %>
                  </p>
                </div>
              <% end %>
              <% if notifications[:caution] %>
                <div class="p-notification--caution">
                  <p class="p-notification__response">
                    <span class="p-notification__status"><%= notifications[:caution][:status] %></span>
                    <%= notifications[:caution][:message] %>
                  </p>
                </div>
              <% end %>
              <% if notifications[:success] %>
                <div class="p-notification--positive">
                  <p class="p-notification__response">
                    <span class="p-notification__status"><%= notifications[:success][:status] %></span>
                    <%= notifications[:success][:message] %>
                  </p>
                </div>
              <% end %>
              <% if notifications[:info] %>
                <div class="p-notification--information">
                  <p class="p-notification__response">
                    <span class="p-notification__status"><%= notifications[:info][:status] %></span>
                    <%= notifications[:info][:message] %>
                  </p>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <footer>
    </footer>
  </body>
</html>
