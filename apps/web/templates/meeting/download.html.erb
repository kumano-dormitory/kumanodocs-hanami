<div class="p-strip">
  <div class="row">
    <h2>会議資料のPDF版のダウンロード</h2>
    <div class="p-notification--caution">
      <div class="p-notification__content" role="status">
        <p class="p-notification__title">注意:</p>
        <p class="p-notification__message">資料委員会の印刷用PDFは、資料委員会向け管理ページからダウンロードしてください.</p>
      </div>
    </div>
    <p>会議を選んでください</p>
    <ul class="p-list--divided">
      <% meetings.each do |meeting| %>
      <li class="p-list__item">
        <a href='<%= routes.download_meeting_path(id: meeting.id) %>' class="p-button--base u-no-margin--bottom">
          <h4 class="u-no-margin--bottom"><%= meeting.formatted_date %>の<% if meeting.type == 0 %>ブロック会議<% elsif meeting.type == 1%>寮生大会<% elsif meeting.type == 2%>代議員会<% elsif meeting.type == 3%>寮生集会<% end %></h4>
        </a>
      </li>
      <% end %>
    </ul>
  </div>
</div>
