<div class="p-strip">
  <div class="row">
    <h2>ブロック会議資料のPDF版のダウンロード</h2>
    <div class="p-notification--caution">
      <p class="p-notification__response" role="status">
        <span class="p-notification__status">注意:</span>資料委員会の印刷用PDFは、資料委員会向け管理ページからダウンロードしてください.
      </p>
    </div>
    <p>ブロック会議を選んでください</p>
    <ul class="p-list--divided">
      <% meetings.each do |meeting| %>
      <li class="p-list__item">
        <a href='<%= routes.download_meeting_path(id: meeting.id) %>' class="p-button--base u-no-margin--bottom">
          <h4 class="u-no-margin--bottom"><%= meeting.formatted_date %>の<%= meeting.type == 1 ? "寮生大会" : "ブロック会議" %></h4>
        </a>
      </li>
      <% end %>
    </ul>
  </div>
</div>
